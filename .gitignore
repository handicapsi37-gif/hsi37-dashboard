# Fichiers de configuration locaux — contiennent les clés Supabase, ne jamais envoyer sur GitHub
config.js
secrets_import.py

# Fichiers système macOS (tous les sous-dossiers)
.DS_Store
**/.DS_Store

# Dépendances Node.js (si npm est utilisé un jour)
node_modules/
